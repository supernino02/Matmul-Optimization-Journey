# matmul-optimization-journey
An iterative approach to maximizing matrix multiplication performance through cache-aware sequential optimizations, shared-memory (OpenMP), GPU-acceleration (CUDA), and distributed-memory (MPI).
